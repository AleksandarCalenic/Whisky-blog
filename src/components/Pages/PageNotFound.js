import classes from '../../style/PageNotFound.module.css';

const PageNotFound = () => {
    return ( 
        <section>
            <h2 className={classes['not-found']}>Page not found</h2>
        </section>
     );
}
 
export default PageNotFound;